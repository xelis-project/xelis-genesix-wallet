name: CI Checks (Dev)

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-checks-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Run Reusable Checks
    uses: ./.github/workflows/ci_checks_reusable.yml
    with:
      dart_analyze_args: --no-fatal-warnings
